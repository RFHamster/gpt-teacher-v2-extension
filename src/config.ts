export const config = {
    // Backend API URL
    BACKEND_URL: 'http://127.0.0.1:8000',

    // API Endpoints
    API_ENDPOINTS: {
        LOGIN: '/api/v1/auth/login/access-token',
        LOGOUT: '/api/auth/logout',
        ITEMS: '/api/items',
        CHAT: '/api/chat'
    }
};
